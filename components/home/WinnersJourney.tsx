import React from "react";
import { layout } from "../style";

const WinnersJourney = () => {
  return (
    <section className={`flex justify-center bg-primary max-h-fit  `}>
      <img src={`/journey.jpg`} alt="" className="px-auto" />
    </section>
  );
};

export default WinnersJourney;
